const initialState = {
  recipies: [],
  isFetching: false,
  isEditing: false,
}

export const recipeReducer = (state = initialState, action) => {
  switch(action.type){
    default: return state
  }
}